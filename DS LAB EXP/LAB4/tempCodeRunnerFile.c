          // case 2:
            // displaylist();
            // break;

            // case 3:
            // prinft("enter the value to insert: ");
            // scanf("%d", &value);
            // inserthead(value);
            // break;

            // case 4:
            // prinft("enter the value to insert: ");
            // scanf("%d", &value);
            // insertend(value);
            // break;

            // case 5:
            // printf("enter the value to insert: ");
            // scanf("%d", &value);
            // printf(" enter the position to insert the value at: ");
            // scanf("%d", &position);
            // insertatposition(value, position);
            // break;

            // case 6:
            // deletefromstart();
            // break;

            // case 7:
            // deletefromend();

            // case 8:
            // printf("enter position to dlt: ");
            // scanf("%d", &position);
            // deletefromposition(position);
            // break;

            // case 9:
            // printf(" totoal number of nodes: %d\n", countnodes());
            // break;

            // case 10:
            // printf(" enter elemnt to search: ");
            // scanf("%d", &value);
            // search(value);
            // break;

            // case 11:
            // exit(0);